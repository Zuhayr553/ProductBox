import 'package:flutter/material.dart';

class MyThemeData {
  static final themeData = ThemeData(
    backgroundColor: Color(0xFF293462),
    primaryColor: Color(0xFF00818A),
    primaryColorLight: Color(0xFF216583),
    primaryColorDark: Color(0xFF293462),
  );
  Color kWhite = Colors.white;
  Color kBackground = const Color.fromARGB(255, 41, 52, 98);
  Color kText = Color.fromARGB(255, 85, 85, 85);
  Color kButtonBack = const Color.fromRGBO(0, 129, 138, 2);
}
